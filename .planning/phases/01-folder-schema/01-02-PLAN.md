---
phase: 01-folder-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/db/server/api/v1/folders/index.get.ts
  - packages/db/server/api/v1/folders/index.post.ts
  - packages/db/server/api/v1/folders/[id].patch.ts
  - packages/db/server/api/v1/folders/[id].delete.ts
autonomous: true
requirements:
  - FOLDER-01
  - FOLDER-02
  - FOLDER-03
  - FOLDER-05
must_haves:
  truths:
    - User can create a folder at any nesting depth via API
    - User can rename an existing folder via API
    - User can delete a folder (and its contents cascade delete) via API
    - API accepts folder_id parameter for folder operations
  artifacts:
    - path: packages/db/server/api/v1/folders/index.get.ts
      provides: GET /api/v1/folders - list user folders
      exports: [GET]
    - path: packages/db/server/api/v1/folders/index.post.ts
      provides: POST /api/v1/folders - create folder
      exports: [POST]
    - path: packages/db/server/api/v1/folders/[id].patch.ts
      provides: PATCH /api/v1/folders/[id] - rename folder
      exports: [PATCH]
    - path: packages/db/server/api/v1/folders/[id].delete.ts
      provides: DELETE /api/v1/folders/[id] - delete folder
      exports: [DELETE]
  key_links:
    - from: packages/db/server/api/v1/folders/*.ts
      to: packages/db/server/services/folder.service.ts
      via: import { createFolder, renameFolder, deleteFolder, getFolders }
---

<objective>
**Phase 1, Plan 2:** FastAPI CRUD endpoints for folder management

Purpose: Expose folder service methods via REST API endpoints following Nuxt Nitro patterns.

Output: Four API routes (GET folders, POST create, PATCH rename, DELETE delete).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-folder-schema/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET folders list endpoint</name>
  <files>packages/db/server/api/v1/folders/index.get.ts</files>
  <action>
Create GET /api/v1/folders endpoint:

```typescript
export default defineEventHandler(async (event) => {
  // Get authenticated user from session
  const session = await getServerSession(event)
  if (!session?.user?.id) {
    throw createError({ statusCode: 401, message: 'Unauthorized' })
  }

  // Get folders (global + user-owned)
  const result = await folderService.getFolders(session.user.id)
  
  if (result.error) {
    throw createError({ statusCode: 500, message: result.error })
  }
  
  return result.data
})
```
  </action>
  <verify>
    <automated>grep -n "defineEventHandler" packages/db/server/api/v1/folders/index.get.ts</automated>
  </verify>
  <done>GET /api/v1/folders returns user's folders (global + owned)</done>
</task>

<task type="auto">
  <name>Task 2: POST create folder endpoint</name>
  <files>packages/db/server/api/v1/folders/index.post.ts</files>
  <action>
Create POST /api/v1/folders endpoint:

```typescript
export default defineEventHandler(async (event) => {
  const session = await getServerSession(event)
  if (!session?.user?.id) {
    throw createError({ statusCode: 401, message: 'Unauthorized' })
  }

  const body = await readBody(event)
  
  // Validate with Zod
  const parsed = folderCreateSchema.safeParse(body)
  if (!parsed.success) {
    throw createError({ 
      statusCode: 400, 
      message: 'Invalid input',
      data: parsed.error.errors 
    })
  }

  // If isGlobal, set ownerId to 'global'
  const input = {
    ...parsed.data,
    ownerId: parsed.data.isGlobal ? 'global' : session.user.id
  }

  const result = await folderService.createFolder(input)
  
  if (result.error) {
    throw createError({ statusCode: 500, message: result.error })
  }
  
  return result.data
})
```

Zod schema:
```typescript
const folderCreateSchema = z.object({
  name: z.string().min(1).max(255),
  parentId: z.string().uuid().optional(),
  isGlobal: z.boolean().optional().default(false)
})
```
  </action>
  <verify>
  <automated>grep -n "folderCreateSchema" packages/db/server/api/v1/folders/index.post.ts</automated>
  </verify>
  <done>POST /api/v1/folders creates new folder, validates input</done>
</task>

<task type="auto">
  <name>Task 3: PATCH rename folder endpoint</name>
  <files>packages/db/server/api/v1/folders/[id].patch.ts</files>
  <action>
Create PATCH /api/v1/folders/[id] endpoint:

```typescript
export default defineEventHandler(async (event) => {
  const session = await getServerSession(event)
  if (!session?.user?.id) {
    throw createError({ statusCode: 401, message: 'Unauthorized' })
  }

  const folderId = getRouterParam(event, 'id')
  const body = await readBody(event)
  
  const parsed = folderRenameSchema.safeParse(body)
  if (!parsed.success) {
    throw createError({ 
      statusCode: 400, 
      message: 'Invalid input',
      data: parsed.error.errors 
    })
  }

  const result = await folderService.renameFolder(
    folderId,
    session.user.id,
    parsed.data.name
  )
  
  if (result.error) {
    throw createError({ statusCode: 500, message: result.error })
  }
  
  return result.data
})
```
  </action>
  <verify>
  <automated>grep -n "renameFolder" packages/db/server/api/v1/folders/\[id\].patch.ts</automated>
  </verify>
  <done>PATCH /api/v1/folders/[id] renames folder</done>
</task>

<task type="auto">
  <name>Task 4: DELETE folder endpoint</name>
  <files>packages/db/server/api/v1/folders/[id].delete.ts</files>
  <action>
Create DELETE /api/v1/folders/[id] endpoint:

```typescript
export default defineEventHandler(async (event) => {
  const session = await getServerSession(event)
  if (!session?.user?.id) {
    throw createError({ statusCode: 401, message: 'Unauthorized' })
  }

  const folderId = getRouterParam(event, 'id')
  
  const result = await folderService.deleteFolder(folderId, session.user.id)
  
  if (result.error) {
    throw createError({ statusCode: 500, message: result.error })
  }
  
  return { deleted: true }
})
```
  </action>
  <verify>
  <automated>grep -n "deleteFolder" packages/db/server/api/v1/folders/\[id\].delete.ts</automated>
  </verify>
  <done>DELETE /api/v1/folders/[id] deletes folder with cascade</done>
</task>

</tasks>

<verification>
- [ ] GET /api/v1/folders returns user folders
- [ ] POST /api/v1/folders creates folder
- [ ] PATCH /api/v1/folders/[id] renames folder
- [ ] DELETE /api/v1/folders/[id] deletes folder
- [ ] All endpoints validate auth
- [ ] All endpoints return proper error codes
</verification>

<success_criteria>
- User can list their folders (global + owned)
- User can create nested folders
- User can rename folders they own
- User can delete folders
- All endpoints return proper HTTP status codes
</success_criteria>

<output>
After completion, create `.planning/phases/01-folder-schema/01-02-SUMMARY.md`
</output>